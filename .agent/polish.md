# Polish register

Deferred off-spine items; `/session-polish` = sole consumer (pick by `pri`, size-fit S/M/L, prune row on completion, mark `stale(<why>)` in place). Row = `pri size title — defect. files: … accept: …`; accept additionally implies project gates green under MAIN rerun (`uv run pytest` + `uv run ruff check .`, counts read per memory `Gate honesty`).

Current items = dashboard focus/story-pass deferrals (richer anchors: `.scratch/agents/map-task-1.md` §7 while it lives; rows below are self-contained). Shared bound, all rows: reviewed finding reading/basis prose stays byte-untouched (its <480/>200 char gate is test-enforced).

- pri=1 size=S outcome-driven prediction headings — patient + simulator fixed H2s say "discharge SCIM-III" while the outcome selector offers 11 heads; a selected non-SCIM story looks broken. files: `src/rehab_sci/dashboard/tabs/patient.py` (prediction card), `src/rehab_sci/dashboard/tabs/simulator.py` (result card). accept: selecting a non-SCIM outcome (e.g. AIS, LOS) in each tab renders that outcome's name in the card heading; no fixed outcome string remains in either heading.
- pri=2 size=M simulator quick-profile intake — 25-field wall before the answer. files: `src/rehab_sci/dashboard/tabs/simulator.py`. accept: default intake exposes ~6–8 high-importance fields, remainder collapsed per clinical group; a blank field still means missing (semantics unchanged, zero-input cohort-baseline note intact).
- pri=3 size=M methods per-outcome selector — per-outcome disclosures dump 11 repeated cards (tab ~60 cards/92 graphs behind details). files: `src/rehab_sci/dashboard/tabs/methods.py`. accept: each section renders one outcome at a time behind an outcome selector; an open section shows a single outcome's cards/graphs.
- pri=4 size=S patient/simulator deck mode labels — duplicate question-group decks carry no mode distinction. files: `src/rehab_sci/dashboard/tabs/patient.py`, `src/rehab_sci/dashboard/tabs/simulator.py`. accept: patient deck labelled observed-episode mode, simulator deck labelled hypothetical-profile mode, each cross-links the other tab.
- pri=5 size=S archetype filter placement — model-derived archetype filter sits in the observable cohort-explorer filter bar. files: `src/rehab_sci/dashboard/tabs/overview.py`. accept: filter bar carries observable fields only; archetype filter relocated or explicitly labelled model-derived.
- pri=6 size=S scorecard task/metric labels — scorecard invites cross-outcome score comparison. files: `src/rehab_sci/dashboard/tabs/methods.py`. accept: every scorecard entry names its task + metric.
- pri=7 size=S data-quality badge — scorecard lacks a data-quality signal. files: `src/rehab_sci/dashboard/tabs/methods.py`, `src/rehab_sci/data/quality.py`. accept: badge sourced from the clinical-consistency report renders beside the scorecard.
- pri=8 size=S ISNCSCI worksheet nesting — 132-cell worksheet renders directly inside the first-level disclosure. files: `src/rehab_sci/dashboard/tabs/patient.py`. accept: worksheet sits behind its own "advanced exam" disclosure nested inside the existing ISNCSCI disclosure.
- pri=0 size=M semantic-branch finding-copy gating — overview lead states artifact-contingent facts as fixed strings: ladder domain pattern (bottom=mobility/top=self-care), improve non-monotonicity, best-measure modality, PI-shrink direction; an artifact refresh flipping any of them renders stale claims while numeric source-binding tests stay green (rev-1 M3; certainty figure −shrink annotation already sign-guarded in `figures/overview.py`). files: `src/rehab_sci/dashboard/tabs/overview.py`, `schema/ui_strings.yaml`, `tests/test_dashboard_smoke.py`. accept: derived semantic facts (ladder_domain_pattern, improve_is_nonmonotone, best.modality, pi_shrink sign) select keyed true/false/fallback copy; unit tests over mutated compact facts flip every branch + assert the rendered copy tracks; gates green.
- pri=9 size=S findings-lead visible-text closed set — block-internal child counts now gated, but visible prose added to the lead section outside blocks/disclosures passes unnoticed (rev-1 M4 residue). files: `tests/test_dashboard_smoke.py`. accept: test enumerates every visible text node in the findings lead outside html.Details + asserts membership in the intended set (H2/deck/scope stats/tile claims/kicker/claim/scope line); gates green.
- pri=2 size=M visual-QA harness port — both browser tools backing UI claims live in gitignored `.scratch/uiqa/` (`shot.py` screenshots, `fontaudit.py` the CDP host-fallback sweep that found the button regressions), so no committed state reproduces a rendered check. `playwright` is not even a dev dep — the audit runs under `uv run --with playwright`. files: `scripts/`, `pyproject.toml` (dev group), `docs/project-reference.md` (cheat sheet), `.agent/memory.md` (Visual QA). accept: `uv run python scripts/03_capture_ui.py <outdir> <tag> --lang --tab --wait --sel` renders to PNG and `scripts/04_audit_fonts.py` exits non-zero on any `isCustomFont: false` glyph, both from committed state; cheat sheet lists them; memory's scratch-path notes replaced by committed paths.
- pri=6 size=S methods column measure — at ≥1180px the scorecard table + prose occupy ~60% of the shell and the right third stays empty, reading as a broken grid rather than a set measure. files: `src/rehab_sci/dashboard/assets/style.css` (methods rules). accept: the Methods tab either fills the shell or centres a deliberate reading measure; no full-width container with a permanently empty right third.
- pri=7 size=S JA comparator inconsistency — JA strings mix `≧` (U+2267, `MMT≧3`, `Δ≧1分節`) and `≥` (U+2265, `≥1段階改善`) for the same relation; both now ship a glyph, so this is copy consistency, not coverage. files: `schema/ui_strings.yaml`. accept: JA strings settle on one comparator throughout (either is defensible in JA technical prose); EN strings keep `≥`; gates green.
- pri=6 size=S simulator action buttons overflow their box — `例示用の典型プロファイルを入力` needs 187px of a 137px inner box, so it wraps to 3 lines and breaks inside the katakana word プロファイル. Font-independent (15 CJK chars are em-square in any face); the buttons are two `flex: 1` halves of a narrow column. files: `src/rehab_sci/dashboard/assets/style.css` (`.sim-action-btn`, `.sim-input-actions`). accept: both JA labels render on one line at 1600px and 900px without mid-word katakana breaks (shorter label, stacked layout, or `line-break: strict`); EN unaffected.
- pri=9 size=S mono 400 face ships unused — every `var(--font-mono)` rule resolves to weight 600, so `ibm-plex-mono-400.woff2` (19.8 KB) is never requested, against the build's own "ship exactly the weights the CSS asks for". files: `scripts/02_build_fonts.py`, `tests/test_font_coverage.py`. accept: either the face is dropped from `FAMILIES` or a rule genuinely uses mono 400; a gate asserts declared weights == used weights so dropping one cannot silently round a future 400 request up to 600.
- pri=7 size=S single-record patient episode picker — the duplicate-registration merge makes every identified patient hold exactly one record, so the patient explorer's record selector always renders one option. files: `src/rehab_sci/dashboard/tabs/patient.py`, `src/rehab_sci/data/episodes.py`. accept: selector renders only when a patient carries >1 record; a single-record patient shows the record id as static text, and the data-driven multi-record path stays intact for future data.
- pri=6 size=S one-command artifact regeneration — full regeneration is 13 cheat-sheet commands run in a required order (archetypes after train), reconstructed by hand each time and easy to run partially. files: `scripts/`, `docs/project-reference.md` (§6). accept: `uv run python scripts/03_regenerate_all.py [--from <step>]` runs the sequence in order, aborts on the first non-zero step, prints per-step timing; cheat sheet points at it; committed state reproduces every tracked metric JSON.
- pri=6 size=S finding-01 claim vs ladder legend vocabulary — the claim names SCIM-III subscales (`Mobility` / `移動`, `schema/scim_iii.yaml`) while the figure legend groups the same bars by the G7 four-domain split (`Ambulation` + `Transfers & bed mobility` / `歩行` + `移乗・ベッド上動作`), so the low-end noun matches row labels but no legend swatch, and the reading prose underneath runs on a third wording (`ambulation and transfer items`). One consequence is live: bed mobility belongs to the claim's subscale yet ranks 6th from the top at 62%, so a subscale-as-block reading of the claim is wrong where an extremes reading is right. files: `schema/ui_strings.yaml` (`overview_finding_ladder_title`, `ind_domain_*`), `src/rehab_sci/dashboard/figures/overview.py`. accept: one vocabulary governs the block — legend swatches carry the subscale grouping, or the claim names the domains it means — and every noun in the visible claim matches on-chart text in both langs; reviewed reading/basis prose stays inside its char gate; rendered check at 1600×1000 both langs; gates green.
- pri=7 size=S JA phrase breaking stops at the overview findings lead — `word-break: auto-phrase` + `lang=` reach `_finding_block` and the glance band alone, so every other JA surface (findings-lead deck, patient/simulator/methods/insights copy) still wraps per character and can split a katakana word mid-token. files: `src/rehab_sci/dashboard/assets/style.css`, `src/rehab_sci/dashboard/tabs/*.py`, `src/rehab_sci/dashboard/app.py`. accept: the content language is declared once at a container every tab inherits (or `<html lang>` tracks the toggle via clientside callback), the phrase-breaking rule covers JA prose surfaces rather than one heading selector, and a rendered check at 1600×1000 finds no mid-token katakana or numeral+counter break in JA on any tab; EN wrapping byte-identical; gates green.
