Continue project. First load `.agent/roadmap.md` + `.agent/memory.md`; either missing ⇒ create a minimal stub (roadmap: goal + first UNPLANNED milestone from available context; memory: empty) before proceeding. Task present ⇒ execute exactly it; update other files as needed for consistency. Task empty ⇒ run MODE for the active milestone (first awaiting DONE/REVIEWED).

MODE ← active-milestone status (state-changing closes use a scoped commit; an unchanged BLOCKED recheck closes read-only; convention below):
- UNPLANNED (incl. a still-unsplit future milestone) → PLANNING
- IN-PROGRESS (has an OPEN unit) → WORK-UNIT (lowest OPEN unit)
- IN-PROGRESS (OPEN=0; BLOCKED>0) → WORK-UNIT (lowest BLOCKED, gate recheck)
- IMPLEMENTED (all units DONE; review pending) → MILESTONE-REVIEW

Isolation — worktree mechanics per global `CLAUDE.md` `Subagents`; project bindings: one worktree per teammate at `.scratch/worktrees/<name>`, MAIN alone writing the primary tree, gate toolchain-env recipe → memory.
- Worktrees deliver evidence — suites, oracle bytes, matrices, probe results; the primary tree ships. `prod` harvest alone merges content: MAIN reads `git diff --stat main..wt/<name>` then targeted files, spot-checks the contract's invariant surfaces even on a clean validator run → one scoped commit.

Execution map — MAIN implements + rules; teammates prepare MAIN's decisions, absorb bulk reading, attack MAIN's product, and harden every ruling into a mechanical gate.
- Authorship split: judgment-bearing code (kernel/validator/spec/normative surfaces) = MAIN-authored in the primary tree, start to ship; mechanically-gated artifacts (validator-backed corpus/fixtures/goldens/tables) = teammate production. Teammate output enters through exactly three trust shapes: execution-verified (suites/scripts/repros MAIN reruns), attention-directing (maps/flags/findings MAIN validates before acting — wrongness costs attention only), raw material (drafts/digests MAIN edits into shipped form).
- MAIN's window buys: acceptance contract, batch verdicts on prepared decision points, implementation, divergence + finding rulings, decisive-gate reruns, commits, MAIN-retained files. Bulk payloads (failure logs, transcripts, long reports, green runs) route through a teammate digest w/ pointers; digests = browse context.
- MAIN-retained files = `.agent/roadmap.md`, `.agent/memory.md`, `CLAUDE.md`, command/skill files. A teammate drafts a delta into `.scratch/agents/<name>.md`; MAIN reviews it and applies the durable edit.
- Traffic terminates at MAIN: findings, oracle results, digests land in reports MAIN polls; MAIN alone dispatches follow-ups.
- Portfolio (`<role>-m<m>[u<u>][-<k>]`):
  - `map` = code/spec surface map + anchored normative checklist (`file:line` per obligation) + git archaeology (analog units; SHAs = checkable pointers). `res` = bounded question set + explicit WebSearch allowance + the `Authenticated web` route for authenticated/paywalled sources.
  - `spike` = one design alternative validated on real pipeline probes in its worktree; MAIN picks; the decision record enters the contract.
  - `test` = diff-blind (contract + normative docs only). Phase 1 `<NAME>-PREP-<i>`: enumerate edge inputs + divergent contract readings ⇒ MAIN batch-rules expected outcomes — the verdict table becomes probe corpus + contract clarifications. Phase 2 `<NAME>-DONE-<i>`: encode every verdict + own adversarial probes as the red suite MAIN implements against.
  - `orc` = reference implementation of the identical contract in its worktree — differential instrument: divergences vs MAIN = review targets, bytes stay evidence.
  - `prod` = validator-gated artifact production: MAIN ships the contract's predicates as an EXECUTABLE validator before dispatch, the brief names its command, and the teammate reports once its own output passes. `doc` = drafts from contract + shipped code; MAIN edits into place.
  - `diff` = cross-checks as committed scripts + outputs (credited by MAIN rerun): MAIN impl vs each oracle over the probe corpus (output-hash where deterministic), red suite × every impl.
  - `rev` + `rev2` = adversarial review of MAIN's diff on declared complementary lenses — correctness/spec + claim soundness + guarantee-vs-claim gaps + CLAUDE.md conformance | mutation campaign (mutate touched predicates, expect a committed red per mutant) | determinism sweeps. A claimed defect ships as a red test in the reviewer's worktree, red pre-fix + green post-fix; findings a test leaves unexpressed ⇒ council rule: ≥2 independent lenses confirm ⇒ accept; unique ⇒ MAIN validates by probe before acting.
  - `triage` = on-demand failure intake: full log → minimal repro + bisection + cause hypothesis; the repro's rerun is its own credential.
  - `audit` = claim replayer (MILESTONE-REVIEW). `scout` = next unit's probe/corpus surface. `gate` = standing gate-strength track (property tests, differential oracles, mutation automation) — each increment moves one MAIN judgment into the gate.
- Brief = role + scope + contract + worktree path + marker `<NAME>-DONE-<i>` + report destination (`.scratch/agents/<name>.md`) + deliverable-unit total + batch size (global `CLAUDE.md` deliverable-first checkpointing) + the validator command its output passes + every upstream id/artifact the task consumes (inlined or pathed — a missing id otherwise becomes a cross-agent hunt) + the directive that questions and follow-ups route to MAIN alone (`SendMessage` to a peer is out of scope; it silently resumes a stopped agent with its write allowlist live) + `read-and-conform-to` set: gates (the project's format/lint/type-check/test entry points + its quality-gate command), oracles (test suite + corpus, benchmark/demo entry points), scope sources (roadmap, memory, project scope + spec docs) ⇒ a teammate judging one of these wrong reports it and MAIN rules. Batch independent spawns in one block; dependent spawns form waves. A supplied task runs MAIN-direct; teammates fan out on the user's request.
- Speculation is cheap under abundant teammate capacity — fund `scout`, `gate`, post-acceptance fuzz/property campaigns that escalate on a find. Speculation yields provider capacity to the active wave, unit close proceeds without it, and each result distills into roadmap/gate at harvest or expires.
- CLAUDE.md enforcement: every mode assigns a review teammate the project-CLAUDE.md-conformance lens — Authoring rules on every touched durable file + Engineering practices on code — reported as findings.
- Roster at `.scratch/agents/roster.md` from first spawn (append via `tee -a … <<'EOF'`), one row per INSTANCE — name | worktree | transcript (`.agent/context-gauge.sh -p <name>`) | marker | watcher bg-task id | last gauge | flushed units (n/total) | status | report. Poll every teammate in one loop from it, writing gauge + flushed-unit count on every pass; retire a row's watcher in the same call that stops or supersedes its teammate.
- Hygiene follows the global `CLAUDE.md` `Subagents` protocol (scoping, marker polling, revive/death cadence, report-file confirmation, `TaskStop` at harvest). Project deltas: gauge = `.agent/context-gauge.sh <name>`; successor = `<role>-m<m>u<u>-<k+1>` inheriting report + accepted findings + its predecessor's worktree, whose paths its brief names.
- Close order = global `CLAUDE.md` `Subagents`, run at every commit ending a wave; each mode's close bullet below adds only its own deltas, and the roster is the quiescence sweep's row set.
- Verification: MAIN reruns the decisive gates itself and credits evidence tracing permitted real inputs. Campaign artifacts (matrices, kill lists, sweeps) = committed script + output, credited by rerun. Re-derive a teammate artifact before crediting it — a `.scratch/agents/` PASS from a pre-repair copy certifies the OLD state. A checkpoint claim inherited across a compaction boundary counts as unvalidated ⇒ re-derive from source before acting on it.
- Context: per the project `CLAUDE.md` policy MAIN checkpoints (roster + state) before each compaction boundary and continues. WORK-UNIT closes each unit inside one window and records usage at close; after a full close it chains to the next OPEN unit while used + projected MAIN cost stay under the one-window aim.

PLANNING — split scope into milestones as needed; plan the next milestone.
- Read the prior milestone's commit range + recorded gauges; for the first planned milestone read the scope-seed commit(s) the roadmap names. Size every unit to fit MAIN's implementation + coordination inside the one-window aim — against the nearest analog's `main=` (MAIN-implemented units) or `impl=` (earlier delegated units ≈ implementation load).
- MAIN confirms each milestone precondition through project pipeline/tooling. Met ⇒ clear stale standing block + continue. Unmet ⇒ record standing block + evidence; changed record ⇒ commit `roadmap (M<m> block): …`; unchanged record ⇒ read-only close.
- Wave 1 (one block): `map-m<m>` + parallel `res-m<m>-<i>`, one bounded question set each. Wave 2: `plan-m<m>` drafts the unit split from map + research, tagging kernel/spec-critical units `oracle` (adds `orc` + the full campaign battery) + validator-gated artifact units `prod` (teammate-produced behind validators). Wave 3: `planrev-m<m>` attacks the draft (sizing realism vs recorded actuals, dependency order, gate coverage, missed scope, parallelizability, CLAUDE.md conformance). MAIN arbitrates + writes the roadmap; planning holds sole sizing authority, so every unit ships as scoped.
- Sequence gate-independent prep first; mark a gated unit BLOCKED until its precondition is met.
- Close per Close order: set the milestone IN-PROGRESS (units enumerated), commit `roadmap (M<m> plan): …`.

WORK-UNIT.
- Read the last completed unit's commit(s), or the planning commit(s) for the milestone's first unit.
- Precondition transition: recheck BLOCKED first. Met ⇒ clear block, set OPEN, continue. Unmet ⇒ retain BLOCKED; materially changed evidence ⇒ update + commit `roadmap (M<m>.<u> block): …`; stable evidence ⇒ read-only close. OPEN + unmet ⇒ set BLOCKED, record condition/evidence, make the block commit, close.
- Wave 1 (one block): `map-m<m>u<u>`; material design fork (constructor/naming/spec surface) ⇒ `spike-m<m>u<u>-<alt>` per alternative. MAIN picks from spike evidence and writes the acceptance contract — testable predicates + invariant surfaces + gate identity + probe-corpus seed; every downstream artifact decides against it.
- Wave 2 (one block, contract attached): `test-m<m>u<u>`; `orc-m<m>u<u>` on `oracle` units; `prod-m<m>u<u>` for the unit's validator-gated artifacts; `rev-m<m>u<u>` + `rev2-m<m>u<u>` prep (`<NAME>-PREP-<i>`: contract + normative docs → adversarial probe matrix + acceptance checklist).
- MAIN batch-rules the `test` phase-1 table, then implements in the primary tree against the delivered red suite, harvesting each case as it drives it green (a wrong red ⇒ inspect, rule, correct suite or contract); failure logs + intermediate runs route through `triage-m<m>u<u>`.
- Suite green ⇒ `diff-m<m>u<u>` cross-checks; MAIN rules each divergence with authorship knowledge — oracle consensus against MAIN's reading indicts the contract ⇒ correction lands in verdict table + red suite first, code second. MAIN then sends `rev`/`rev2` the design record + diff pointer ⇒ red-test review (`<NAME>-DONE-<i>`); MAIN fixes its own code; each accepted finding closes on its acceptance check under MAIN's rerun; re-review stays for a fix no check decides.
- Close per Close order: record `main=` (`.agent/context-gauge.sh`; carries implementation) + `mate=` (`.agent/context-gauge.sh <name>`, high-water, peak across teammates), both `N% NK/240K`, in the roadmap; set the unit DONE and — once all units are DONE — the milestone IMPLEMENTED; commit `<scope> (M<m>.<u>): …`; then apply the chain rule.

MILESTONE-REVIEW.
- Read every commit within the milestone.
- One block: per-unit reviewers (that unit's commits + touched surfaces; correctness/spec), one cross-cutting reviewer (cross-unit integration; project-CLAUDE.md conformance incl. instruction/memory token-efficiency + obsolescence), one `audit-m<m>` claim replayer (re-derives every roadmap/docs claim for the milestone: full gate suite, recorded numbers reproduced, unreproducible claims flagged). Each finding supplies severity + `file:line` + divergence + impact + acceptance check, shipping a red test wherever one expresses it.
- MAIN validates + dedupes (council rule); MAIN applies accepted judgment-bearing fixes itself; `prod`-artifact batches go to one fix teammate per cohesive batch (locations + acceptance checks). MAIN's decisive-gate + acceptance-check rerun closes each batch; originating-reviewer re-review stays for a fix no check decides.
- A requirement-changing design reaches the user before any scope-source edit.
- Close per Close order: set the milestone REVIEWED, commit `<scope> (M<m> review): …`. The next session plans the next milestone.

Commit convention — scoped (`<scope>: …`), trace key in parens: unit `(M<m>.<u>)`, block `(M<m> block)` / `(M<m>.<u> block)`, plan `(M<m> plan)`, review `(M<m> review)`. Grep a milestone's history: `git log --grep "(M<m>[. ]"`.

Task: $ARGUMENTS
