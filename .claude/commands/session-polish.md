Spare-capacity session: execute deferred-perfection items from `.agent/polish.md`. Decoupled from the roadmap — milestone + unit state belongs to `/session-roadmap` alone; safe to run anytime, abandon anywhere, repeat.

- Load `.agent/polish.md` + `.agent/memory.md`. Register missing/empty ⇒ report + stop; items originate in other sessions at deferral time.
- Pick by `pri`, then size-fit to the remaining window (S ≈ ≤15%, M ≈ ≤35%, L = the session). An item whose evidence pointer or acceptance check no longer holds ⇒ mark `stale(<why>)` in place + move on; the next `/session-roadmap` session re-rules it.
- Execute one item at a time under the project's normal gates; teammate use, worktrees, hygiene + Close order = session-roadmap rules. An item completes when its acceptance check runs green under MAIN's own rerun → one scoped commit `<scope> (polish): …`, pruning its row in the same commit.
- Boundary: items run inside the artifact's existing assurance tier, unit set + scope sources — the register is this session's sole scope-source edit; tier raises, new units + scope-source changes belong to `/session-roadmap`. A finding implying spine work ⇒ append it as `spine? <finding> | why: <evidence>` + report it to the user.
- Close: register current (done rows pruned, stale rows marked), scoped commits, report = done | skipped(stale) | flagged-spine.

Task: $ARGUMENTS
